let num = 2;
const PI = 3.14;

var numeros = [1,2,3];
numeros.map(function(valor){
    return valor * 3;
});

numeros.map(valor => valor * 3 );

class Matematica {
    soma(x,y){
        return x + y;
    }
}